_schema-version: 3.3.0
ID: test_mp_capm_july_7am
version: 1.0.0
description: "Purchase Order Application"
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: test_mp_capm_july_7am-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: test_mp_capm_july_7am-auth
      - name: test_mp_capm_july_7am-db

  - name: test_mp_capm_july_7am-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: test_mp_capm_july_7am-db

resources:
  - name: test_mp_capm_july_7am-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: test_mp_capm_july_7am-${org}-${space}
        tenant-mode: dedicated
  - name: test_mp_capm_july_7am-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
